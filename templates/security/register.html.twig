{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-4  ">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Register</button>
        {{ form_end(form) }}
    </div>
{% endblock %}

{% block title %}

{% endblock %}

